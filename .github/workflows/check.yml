name: CI Check
on:
  push:
    branches: [main]

jobs:
  structure-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          test -f README.md
          test -d api
          test -d functions
          test -d model
          test -d web
          echo "Project structure validated"
